# AutoPlayNotes 採譜アドオン（音源→MIDI / basic-pitch）の依存ロック
# 実機で E2E 成功した bp_venv の pip freeze（Python 3.12 Windows amd64）を完全ピン。
# build_pitch_addon.py が再配置可能 Python へ `pip install --no-deps -r` でそのまま入れる
# （全依存を固定済みなので resolver を通さない＝basic-pitch が引きずる重い依存を避ける）。
# onnxruntime バックエンド（nmp.onnx）で推論。tensorflow / coreml / tflite は入れない。
# ※ basic-pitch の onnx モデル（nmp.onnx, 0.2MB）は wheel 同梱なので別途の種付けは不要。
# ※ scikit-learn / mir_eval はベンチ由来で実行時には不要な可能性あり（将来プルーニング候補）。
audioread==3.1.0
basic-pitch==0.4.0
certifi==2026.6.17
cffi==2.1.0
charset-normalizer==3.4.8
decorator==5.3.1
flatbuffers==25.12.19
idna==3.18
importlib_resources==7.1.0
joblib==1.5.3
lazy-loader==0.5
librosa==0.11.0
llvmlite==0.48.0
mido==1.3.3
mir_eval==0.8.2
msgpack==1.2.1
narwhals==2.23.0
numba==0.66.0
numpy==2.4.6
onnxruntime==1.27.0
packaging==26.2
platformdirs==4.10.0
pooch==1.9.0
pretty_midi==0.2.11
protobuf==7.35.1
pycparser==3.0
requests==2.34.2
resampy==0.4.2
scikit-learn==1.9.0
scipy==1.18.0
setuptools==80.10.2
six==1.17.0
soundfile==0.14.0
soxr==1.1.0
threadpoolctl==3.6.0
typing_extensions==4.16.0
urllib3==2.7.0
