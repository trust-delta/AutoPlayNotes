# GUI（モダン UI ウィジェット）。ソースから起動する場合は必須:
customtkinter>=5.2
# 楽譜画像トレース入力・画像プレビューに使用:
pillow>=10.0
# MusicXML 読み込み・数字譜画像の OCR は追加依存なしで動きます。
# MIDI 読み込みを使う場合のみ以下をインストールしてください:
mido>=1.3.0
# 五線譜画像の読み取り（OMR）を使う場合のみ（深層学習モデルを使う重い依存です）。
# oemer 0.1.8 は最新の numpy/scipy と非互換のためバージョンを合わせること:
# pip install oemer "numpy<2" "scipy==1.13.*" "opencv-python-headless<4.10"
